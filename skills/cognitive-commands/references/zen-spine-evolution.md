# Zen Spine Evolution History

> Why the spine is 8, not 10. Arif's settled design.

## v1 (2026-07) — Full Numerological 10

```
000_salam  111_tengok  222_fikir  333_jalan  444_apa
555_betul  666_rasa    777_faham  888_adil   999_ingat
```

All slots filled. `/333_jalan` = ORIENT (pathfinding).

## v2 (2026-07-26 mid) — Stripped to 8

```
000_salam  111_tengok  333_forge  555_betul  666_rasa
777_faham  888_adil    999_ingat
```

**Changes:**
- Removed `222_fikir` (REASON) — absorbed into general reasoning. `/222_fikir` was redundant with `/think_deep` and natural conversation.
- Removed `444_apa` (MEANING) — the "why" question felt forced. Human conversations don't need a dedicated command for meaning; `/ask_curious` covers it.
- Changed `333_jalan` → `333_forge` — ORIENT was too passive. Slot 333 is now FORGE (execute, build, make real).

**Rationale:** "Eight numbers. No filler. Each one earns its slot — or it's gone."

## v3 (Final) — Settled

Same as v2. Arif confirmed:
- 8 spine is permanent
- `/333_jalan` removed entirely (no alias, no redirect)
- `/222_fikir` and `/444_apa` will not return
- New verbs (forget, flow, forge, padu) live as bare verbs, not numerological slots

## Stripping Rule

If any future agent considers restoring 222, 444, or 333_jalan:
**DON'T.** They were removed by Arif's explicit design. The 8-spine is settled law. To add a new numerological command, it must earn a NEW slot (e.g., `/111_` or `/555_`) — never revive a stripped number.
