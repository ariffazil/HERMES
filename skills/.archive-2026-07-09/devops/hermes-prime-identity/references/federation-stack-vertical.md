# Federation Stack — Canonical Vertical

> Captured 2026-07-19 from Arif's sovereign statement.
> This is the single-sentence description of every layer.

```
        ARIF  ← sovereign veto, final meaning
          ↓
       999 VAULT  ← what actually happened (immutable receipt)
          ↑
       777 FORGE  ← execution after authority (A-FORGE)
          ↑
       888 APEX  ← consequence admissibility verdict
          ↑
       555 ASI  ← what context is missing? (critique)
          ↑
       333 AGI  ← reason within bounded context
          ↑
       AAA  ← which agent + which context matters now?
          ↑
       MCP ⇄ A2A  ← internal capabilities | discovery/delegation/artifacts
```

**One-line version:**
> MCP gives agents capabilities. A2A lets agents discover and delegate. AAA routes context.
> 333 reasons. 555 critiques. 888 judges consequence. 777 executes. 999 seals. ARIF vetoes.

**The principle:** Intelligence is context selection under constraint. The center is routing. The edges are authority. What survives the chain is intelligence.

**Usage:** Paste this into any agent boot doc, SALAM init, or audit response when Arif asks "what are we building?"
