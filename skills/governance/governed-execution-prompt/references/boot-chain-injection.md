# Boot Chain Injection — Zen Doctrine Propagation

**After compressing AGENTS.md files, ensure EVERY runtime loads the operational zen doctrine.**

## The Gap

You compress `/root/AGENTS.md` from 4,000w → 250w. Every organ's AGENTS.md gets the same treatment. But when Claude Code or Kimi Code wakes up, it reads DITEMPA and starts working — and never sees the 18 operational rules in AAA-ZEN-ALIGNMENT.md.

Only OpenCode explicitly loaded the zen doctrine. Every other runtime was flying blind.

## The Fix: One-Line Boot Chain Pointer

After every AGENTS.md header (right after the DITEMPA line), add:

```
> **ZEN:** `/root/AAA/prompts/AAA-ZEN-ALIGNMENT.md` — 18 operational rules, load at boot.
```

For HERMES-style existing format:
```
> **Canonical AAA:** /root/AAA/CLAUDE.md · **Zen:** /root/AAA/prompts/AAA-ZEN-ALIGNMENT.md
```

## The Doctrine File

`AAA-ZEN-ALIGNMENT.md` contains the 18 operational rules that prevent the 7 known failure patterns (config divergence, probe-before-claim violations, snapshot-before-change, key hygiene, structural permissions, identity binding, witness parity, model governance, skills hygiene, drift detection).

Details: `references/federation-scale-zen-pruning.md` §Step 3 + Pitfall 16.

## Verification

After injection, verify EVERY organ's AGENTS.md or CLAUDE.md:
```
grep -r "AAA-ZEN-ALIGNMENT" /root/*/AGENTS.md /root/AAA/CLAUDE.md /root/*/CLAUDE.md 2>/dev/null
```

Every runtime must appear. Missing one = gap.
