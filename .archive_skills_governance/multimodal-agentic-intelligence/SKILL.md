---
name: multimodal-agentic-intelligence
description: >-
  Crystallised Δ·Ω·Ψ → multimodal agentic intelligence doctrine. Load whenever
  processing multimodal inputs (images, audio, video, sensor), bridging perception
  to governed cognition, or wiring G-fold impact from multimodal evidence.
trigger: multimodal|image analysis|audio processing|video understanding|sensor fusion|agentic multimodal|Δ·Ω·Ψ multimodal
category: governance
---

# Multimodal → Agentic Intelligence — Operational Skill

> **Live doctrine:** `/root/AAA/governance/MULTIMODAL_AGENTIC_INTELLIGENCE.md`  
> **Constitution:** `INTELLIGENCE_CONSTRAINT_PHYSICS.md` · `AGI_ASI_APEX_HUMAN.md`  
> **DITEMPA BUKAN DIBERI**

## When to load

Load this skill when you:
- Process images, audio, video, sensor, or multimodal text
- Bridge an LLM multimodal witness to arifOS governed execution
- Wire G-fold impact from multimodal evidence
- Design a multimodal tool or MCP endpoint
- Audit multimodal claims for F2/F7/F9 compliance

## Key invariants

### 1. LLM = witness, not judge
The LLM proposing a multimodal interpretation is **witness**, not authority. Always label multimodal claims with epistemic tags:
- `OBS` — directly observed in the modality (pixel values, waveform samples)
- `DER` — derived from observation (edge detection, phoneme boundary)
- `INT` — interpreted from derived data (sentiment, geological feature)
- `SPEC` — speculative inference beyond available evidence

### 2. Δ metabolises every modality
Before any multimodal observation enters the governance chain:
1. Extract epistemic content from raw modality
2. Map to A·P·E·X·Φ primitives
3. Measure G impact (`G = A · P · E · X · Φ`)
4. Track ΔS (entropy change from this observation)
5. Label every derived claim

### 3. Ω envelopes every cross-organ artifact
Every multimodal artifact crossing an organ boundary must carry:
```typescript
{
  modality: "image" | "audio" | "video" | "text" | "sensor",
  epistemic_label: "OBS" | "DER" | "INT" | "SPEC",
  g_fold_impact: number | null,
  provenance: string,
  checksum: string  // SHA-256 of raw content
}
```

### 4. Ψ gates every irreversible multimodal action
Before executing from multimodal evidence:
1. AmanahLock — catastrophic pattern scan (F1)
2. Canonical G check — `GET http://localhost:8088/health` → G ≥ 0.50
3. C_dark check — hallucination bound < 0.30 (F9)
4. Lease-gated execution
5. VAULT999 seal after completion

### 5. G-fold is the universal bridge
Multimodal evidence → G-fold impact. Every modality feeds G. If a multimodal observation changes G, the entire federation re-calibrates. If G drops below 0.50, all C4/C5 decisions HOLD.

## Operational flow

```
Multimodal input
  → label epistemic class (OBS/DER/INT/SPEC)
  → decompose into A·P·E·X·Φ primitives
  → measure G impact via arif_think(mode='apex')
  → envelope with typed schema (Ω)
  → route to correct organ
  → judge constitutional (888)
  → execute under lease (Ψ) if SEAL
  → seal to VAULT999
```

## Verification checklist

- [ ] Every multimodal claim has an epistemic label
- [ ] G-fold impact is measured, not assumed
- [ ] C_dark < 0.30 for SEAL-grade multimodal evidence
- [ ] ΔS ≤ 0 (multimodal evidence reduces entropy, not increases it)
- [ ] Ω envelope present on cross-organ multimodal artifacts
- [ ] Ψ lease active before irreversible multimodal action
- [ ] Witness (LLM) never judges — only proposes

## Related skills

- `governed-execution-substrate` — Δ/Ω/Ψ triphasic substrate
- `asi-constitutional-reasoning` — F1–F13 floor application
- `apex-verification-pipeline` — G-fold computation
