# Runtime artifacts
state.db
state.db-shm
state.db-wal
sessions.db
kanban.db
kanban.db.dispatch.lock
kanban.db.init.lock
kanban/

# Caches
cache/
audio_cache/
audio-cache/
image_cache/
sticker_cache.json

# Config backups
config.yaml.bak.*
*.bak

# Browser artifacts
browser_screenshots/
.scratch_tip_shown

# Tuning data
honcho.json

# Curator backups
profiles/*/skills/.curator_backups/

# MCP tokens
mcp-tokens/

# Sensitive content — never push
*caregiver*
*dm-logs*

# Auth / secrets
auth.json
auth.lock

# Code-review quarantine (2026-07-22) — never track
/root/.secrets/quarantine/

# Brevo / SMTP auth references (live creds must be in env only)
*brevo-auth-details*

# Openclaw pre-purge credential backups — never track
*.bak-pre-purge*
.openclaw/openclaw.json.bak-pre-purge*

# Generic credential-bearing files
*credential*secret*
*.openclaw.json.bak*

# Gateway runtime
gateway.lock
gateway.pid
gateway_state.json

# State & sessions
state/
state-snapshots/
checkpoints/
sandboxes/
temporal/

# Logs
logs/
*.log

# Model caches
models_dev_cache.json
ollama_cloud_models_cache.json
provider_models_cache.json

# Runtime dirs
pairing/
platforms/
plugins/
bin/
hooks/
pastes/
plans/
schemas/
scripts/

# State — track specific files, ignore runtime cruft
memories/.legacy/
memories/*.lock
cron/output/
cron/.tick.lock
cron/.jobs.lock
cron/ticker_*
sessions/

# IDE
__pycache__/
*.pyc

# Secrets — never commit
.env
.hermes_history
skills/.curator_backups/
verification_evidence.db

# Runtime state (added 2026-07-21) — agent drift, not source
.update_check
channel_directory.json
processes.json
# config.yaml tracked — no raw secrets, only key_env: env var references
skills/.usage.json
data/pulse/
channel_directory.json

# Backup state — binary restic data, not source
backups/

# Runtime churn (entropy wave2 2026-07-30) — do not track
.restart_last_processed.json
.update_check
gateway/restart_loop.json
skills/.usage.json
.skills_prompt_snapshot.json

# Backup files (per zen sweep 2026-08-01)
*.bak-*
*.bak
verification_evidence.db-shm
verification_evidence.db-wal
skills/.hub/

# Workspace artifacts (added 2026-08-04 stabilization) — not source
scratch/
profiles/default/memories/
.clean_shutdown
